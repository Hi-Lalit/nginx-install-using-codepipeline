version: 0.2

phases:
  install:
    commands:
      - echo Installing Docker
      - apt-get update
      - apt-get install -y docker.io

  build:
    commands:
      - echo Running Docker nginx container
      - docker run -d -p 80:80 --name nginx-container nginx:latest      
      - docker ps